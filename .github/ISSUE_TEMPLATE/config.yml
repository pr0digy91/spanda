blank_issues_enabled: false
contact_links:
  - name: A symbol reports "no callers" but is called at runtime
    url: https://github.com/pr0digy91/spanda#what-it-refuses-to-guess
    about: Read this first — the tool distinguishes "nothing references this" from "I cannot see who does", and the difference is the whole point.
