"""Deliberate syntax error below.

The extractor must record this file as unparseable, with the error and its
line, and carry on with the remaining files. A crash here means one bad
file in a 1000-file codebase takes down the whole index.
"""


def unterminated(value:
    return value
